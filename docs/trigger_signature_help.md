# Функция *triggerSigHelp*
## Назначение функции
Функция принудительно вызывает показ подсказок по вызову процедуры/функции/метода в текущей позиции курсора.

## Параметры функции
* Отсутствуют

## Пример вызова
```javascript
// Вызов подсказок
triggerSigHelp();
```