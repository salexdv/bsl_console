---
issue: <#N | retro | ->
title: <краткое название>
status: draft  # draft | approved | in-progress | done | superseded
owner: <имя>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
area:
  - <сборка | обвязка редактора | грамматика/язык | мост 1С | доставка | ... >
---

# <id>: <краткое название>

> Issue: <ссылка на issue, если есть>

Стабильный контракт: что должно быть реализовано и как вызывающий код/пользователь это видит.
Переживает merge, уезжает в `specs/done/<id>/spec.md`. Транзиентное (этапы, вопросы, решения) — в `plan.md`.

## 1. Контекст и проблема

Зачем это нужно, какую задачу решаем, что сейчас не работает. Ссылки на связанные issue/обсуждения/спеки.

## 2. Область изменений

Какие модули/файлы/слои сборки затрагиваются.

| Компонент | Тип | Характер изменений |
| --- | --- | --- |
| `<файл/модуль>` | src / сборка / конфиг / доки | добавить / изменить / удалить |

## 3. Поведение

### 3.1. Основной сценарий

1. ...

### 3.2. Граничные случаи

- ...

### 3.3. Ошибки

- ...

## 4. Контракты

Публичные интерфейсы, которые меняются или добавляются.

### 4.1. Экспортные функции моста (`window` / `defaultView`)

```js
/**
 * <Описание. Что делает, что возвращает.>
 * @param {Type} имя - описание
 * @returns {Type} описание
 */
function имяФункции(имя) { ... }
```

### 4.2. События (`EVENT_*`) / формат обмена с 1С

| Событие / поле | Направление | Payload | Описание |
| --- | --- | --- | --- |
| `EVENT_<...>` | JS → 1С | `{ ... }` | ... |

### 4.3. Контракт сборки / доставки

Что на выходе (`dist/`), внешние ссылки/ассеты, ограничения движка (ES-floor, инлайн), build-таргеты.

## 5. Критерии приёмки

Проверяемый чек-лист — то, что можно ручно проверить (в браузере, в поле 1С, греп по `dist/`).

- [ ] ...

## 6. Вне области

Явный перечень того, что **НЕ** делается — защита от расползания объёма.

- ...
